#!/usr/bin/env bash
# Filesystem-only resume: node_modules and build artifacts survive, but the
# PostgreSQL must be ready before the database refresh steps. setup starts its
# declared service and skips expensive work whose output is already present.
set -euo pipefail

exec "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/setup"
